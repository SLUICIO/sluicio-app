# SPDX-License-Identifier: Apache-2.0
# Minimal Prometheus config used by docker-compose for local development.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
